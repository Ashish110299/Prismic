import * as prismicH from '@prismicio/helpers'
import linkResolver from './linkResolver'

// Add classes and tags for site title and author name
export const htmlSerializer = (type, element, content, children) => {

}

export default { 
  htmlSerializer, 
}
